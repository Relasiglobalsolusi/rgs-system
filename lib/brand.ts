/** Shared brand assets (cache-bust when replacing logo files). */
export const RGS_ONE_LOGO_SRC = "/rgs-one-logo.png?v=7";

/** Charcoal mark for light surfaces (paired with white-on-dark logo). */
export const RGS_ONE_LOGO_ON_LIGHT_SRC = "/rgs-one-logo-on-light.png?v=3";

/** Intrinsic pixel size of the primary (dark-theme) wordmark. */
export const RGS_ONE_LOGO_WIDTH = 1024;
export const RGS_ONE_LOGO_HEIGHT = 682;

/** Product name for browser chrome, auth, and sidebar. */
export const RGS_ONE_PRODUCT_NAME = "RGS ONE";

/** Browser tab label — pipe between the two words, never doubled. */
export const RGS_TAB_TITLE = "RGS | ONE";

/** Brand tagline shown under the RGS ONE logo. */
export const RGS_ONE_SLOGAN = "ONE PLATFORM. COMPLETE VISIBILITY.";
