-- Intentionally empty. The factory-return work kept Repair on the same asset
-- code, so no `sentAssetCode` column was added. This file only keeps the
-- migration folder valid for `prisma migrate deploy`.
SELECT 1;
